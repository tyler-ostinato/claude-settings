{
  "permissions": {
    "allow": [
      "Bash(npm *)",
      "Bash(npx *)",
      "Bash(yarn *)",
      "Bash(pnpm *)",
      "Bash(node *)",
      "Bash(tsc *)",
      "Bash(git *)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path // .tool_input.new_file_path // empty'); if echo \"$file\" | grep -qE '\\.(ts|tsx|js|jsx|mts|cts)$' && [ -f \"$file\" ]; then npx prettier --write \"$file\" 2>/dev/null || true; npx eslint --fix \"$file\" 2>/dev/null || true; fi"
          }
        ]
      }
    ]
  }
}
